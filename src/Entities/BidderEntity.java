package Entities;

public class BidderEntity extends UserEntity {
    public BidderEntity(Long userId, String userName, String userEmail, String userPassword, String userPhone) {
        super(userId, userName, userEmail, userPassword, userPhone);
    }
}
